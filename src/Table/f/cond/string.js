import {gt, le, eq, gteq, leeq, has, isin} from './operation/i.js';


export default (
    [
        eq,
        
        gt,
        le,
        gteq,
        leeq,

        has,
        isin,
    ]
)