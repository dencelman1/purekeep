import {gt, le, eq, gteq, leeq, floathas, floatin} from './operation/i.js';

export default (
    [
        eq,
        
        gt,
        le,
        gteq,
        leeq,

        floathas,
        floatin,
    ]
);
