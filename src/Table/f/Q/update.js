import { writeSync, readSync } from 'fs';

// W - int[] which values update;
// I - which ids updated ( first length );

export default (
    function(
        E,
        Q,
        W,
        I
    ) {
        return 0;
    }
);
