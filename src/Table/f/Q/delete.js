

export default (
    function(
        Q
    ) {
        // remove from e;
        // add to hole;
        
        for (;;){
            if (
                query(IV,QL, OB, Q,QV, CURRENT_E,CURRENT_SV, cond,logic, readString,readNumber, sdopen,sdadd, d,sd,od)
            ) {

            };
        };
        
        this.L--;
        this.confsave();
        return true;
    }
);
