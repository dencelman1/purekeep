

export default (
    function(l) {
        var
            o = this.arrayFromO2,
            v = (
                (o.length = l),
                Array.from(o, this.NULL)
            ),
            
            o = this.arrayFromO2,
            z = this.z,
            ID = this.ID
        ;

        o.length = this.idsl;
        
        for (
            var i = 0;
            i < l;
            i++
        ) {
            ID.set(
                ID.size,
                ( v[i] = Array.from(o,z) )
            );
        };

        return v;
    }
);