
export default (
    () => {
        var
            SD = (
                sdopen(
                    sd[i],
                    i,
                    (
                        LENGTH = (
                            bfrom[T = r[i]](
                                (
                                    LENGTH_BUFFER = E[i]
                                )
                                , 0
                                , 0
                            )
                        )
                    ),
                    ps,
                    sdadd
                )
            )
        ;
        return (
            (SD[3] += saveField(
                SD[0],
                AR[i],
                LENGTH,
                SD[3]
            )),

            OB.writeUInt16LE(SD[1]++),
            (OP[i] += saveField(od[i], OB, 2, OP[i])),

            saveField(d[i], LENGTH_BUFFER, T, P[i])
        );
    }
)