


function Field( v ) {
    this.l = (
        (
            this.v = v
        )
        .length
    );

    [
        [
            [
                fd,
                holesfd,
            ]
        ], // v
    ];
    [
        [], // l
        [], // o
        [], // v
    ];
};

export default Field;
